# Dependencies
node_modules/
/.pnp
.pnp.js
.yarn/install-state.gz

# Next.js
/.next/
/out/

# Production build
/build

# Tauri 빌드 산출물 (src-tauri 본 구현 단계에서 채워짐)
/src-tauri/target/
/src-tauri/binaries/
/src-tauri/gen/
/dist-tauri/

# WASM 알고리즘 암호화 배포(방법 5, scripts/build/stage-encrypted-wasm.sh) — 빌드마다
# 재생성되는 키/암호화 리소스. native/wasm-engine/.wasm-seed 는 머신당 1회 생성되어 로컬에만
# 남는다(README/plan 참고 — git에 올리는 순간 "암호화"의 의미가 없어진다).
# v2: 키 상수 대신 seed 재료(.wasm-seed)를 두고 HKDF로 런타임 파생한다.
/src-tauri/resources/
/src-tauri/src/wasm_key.rs
/native/wasm-engine/.wasm-key
/native/wasm-engine/.wasm-seed

# WASM 빌드 산출물 — npm run wasm:build 로 재생성 (public/의 소스 파일(mic-processor.js)은 커밋 대상)
/public/wasm/
/public/wasm-debug/
/public/wasm-dummy/

# 로컬 전용 소스 — 원격 레포에는 올리지 않음 (각자 로컬에서만 보관/빌드)
/resources/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Vercel
.vercel

# Measurement
measurements/
test-audio.wav
실험결과-1/
build.log

# 로컬 전용 도구/실험 디렉터리 (프로젝트와 무관)
im-not-ai/
# /humanize-korean 스킬의 회차별 작업 폴더(입력 원문 + 윤문 결과) — 최종본은 각 도메인
# README.md로 이미 커밋되므로 중간 산출물 자체는 추적하지 않는다.
_workspace/

#CLAUDE & iron-device
CLAUDE.md
libirontune.so
/docs
