# ASIO SDK는 빌드 입력으로 로컬 관리한다. 공개 GPLv3 배포판을 사용하더라도
# 버전·원문 라이선스를 명확히 통제하기 위해 이 저장소에는 vendoring하지 않는다.
third_party/

# CMake 작업 폴더 (build.ps1 -Arch 별로 생김)
build-*/

# dist/ 산출물(audio-device-helper.exe)은 GPLv3 ASIO SDK를 링크한 생성 파일이다.
# 패키징 때 소스에서 재현하므로 저장소에는 커밋하지 않는다.
dist/
